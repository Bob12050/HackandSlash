{
  "name": "komorebi-guild",
  "version": "0.5.0",
  "private": true,
  "type": "module",
  "description": "A cute portrait auto-battle RPG built with TypeScript and Phaser 4",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "validate": "npm run test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^7.3.1",
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
