{
  "name": "riftborne",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "A new top-down hack-and-slash built with TypeScript and Phaser 4",
  "scripts": {
    "dev": "vite",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "build": "npm run typecheck && vite build",
    "validate": "npm run test && npm run build",
    "preview": "vite preview"
  },
  "dependencies": {
    "phaser": "4.2.1"
  },
  "devDependencies": {
    "typescript": "7.0.2",
    "vite": "8.1.5",
    "vitest": "4.1.10"
  }
}
